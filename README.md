# Vibe Playlist 🎵

Collaborative music queue. Friends add songs, one person controls playback.

- **`/`** - Share with friends to add YouTube/Spotify songs
- **`/vibe-reproductor-player`** - Control what plays

```bash
bun install && bun run dev
```

Set `DATABASE_URL` and run `bun drizzle-kit push`.
